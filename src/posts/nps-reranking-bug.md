---
title: "TODO: The NPS Re-Ranking Bug"
date: "2026-08-12"
excerpt: "TODO — one-sentence summary once this post is written."
---

**TODO.** This post is not written yet. This is a placeholder so the Writing section has a real entry to show.

What this post needs to cover, once written:

- Why the National Park Service API buried the correct park behind 455 keyword matches
- Why prompting the model harder could not fix a ranking problem that lived in the API response, not in the prompt
- What reading the API's actual ranking behavior revealed
- What the local re-ranking and scoring layer built underneath it actually does

Add real posts as new `.md` files in `src/posts/`. Each file needs a `title`, `date`, and `excerpt` in its frontmatter. No component changes are needed to publish a new post.
